apiVersion: apps/v1
kind: Deployment
metadata:
  name: config-server
  namespace: moniejoint-ns
  labels:
    app: config-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: config-server
  template:
    metadata:
      labels:
        app: config-server
    spec:
      containers:
        - name: config-server
          image: us-central1-docker.pkg.dev/trusty-fuze-456211-j9/microservices/config-server:latest
          ports:
            - containerPort: 8888
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
            requests:
              memory: "256Mi"
              cpu: "200m"
